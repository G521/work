原型模式：
    通过new产生一个对象需要非常繁琐的数据准备或访问权限，则可以使用原型模式。
    就是java中的克隆技术，以某个对象为原型，制造出新的对象。显然，新的对象具备原型对象的特点
    优势有：效率高（直接克隆，避免了重新执行构造过程步骤）
    可镂类似于new 但是不同于new。斌且可露出的新对象改变不会影响原型对象。然后，在修改稿克隆对象的值
实现：
    Cloneable接口和clone方法
    Prototype模式中实现起来最困难的地方就是内存赋值操作，所幸咋iJava中提供了clone（）方法替我们做了绝大部分事情。
